# API base URL for the app (Expo/Rork)
# Required for tRPC. For local development with standalone backend, use port 3001.
# Run backend with: bun run server
EXPO_PUBLIC_RORK_API_BASE_URL=http://localhost:3001

# ACRCloud – music identification (YouTube/SoundCloud URL scanning)
# Get keys: https://www.acrcloud.com/ → Audio & Video Recognition project
# Enable "3rd Party ID Integration" for Spotify/YouTube links in results.
ACRCLOUD_ACCESS_KEY=
ACRCLOUD_ACCESS_SECRET=
ACRCLOUD_HOST=identify-us-west-2.acrcloud.com

# SoundCloud – required for “Identify at timestamp” with SoundCloud URLs
# Create an app at https://developers.soundcloud.com/ → Your Apps → Create → Client ID
SOUNDCLOUD_CLIENT_ID=

# YouTube – required for “Identify at timestamp” with YouTube URLs
# Run: ./scripts/setup-yt-dlp-macos.sh  (downloads standalone macOS binary to ./bin/yt-dlp)
# Then set YT_DLP_PATH to ./bin/yt-dlp or the absolute path. Or use brew install yt-dlp.
# YT_DLP_PATH=./bin/yt-dlp
