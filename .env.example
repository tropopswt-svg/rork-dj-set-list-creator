# API base URL for the app (Expo/Rork)
# Required for tRPC. For local development with standalone backend, use port 3001.
# Run backend with: bun run server
EXPO_PUBLIC_RORK_API_BASE_URL=http://localhost:3001

# YouTube Data API v3 Key
# Get your key from: https://console.cloud.google.com/apis/credentials
# Server-side key (used by Vercel API routes) - DO NOT prefix with EXPO_PUBLIC_
YOUTUBE_API_KEY=your_youtube_api_key_here

# Client-side key (only needed for local development without backend)
EXPO_PUBLIC_YOUTUBE_API_KEY=your_youtube_api_key_here

# Backend API URL (set this to your Vercel deployment URL in production)
# Leave empty for local development to use direct API calls
EXPO_PUBLIC_API_URL=

# ACRCloud – music identification (YouTube/SoundCloud URL scanning)
# Get keys: https://www.acrcloud.com/ → Audio & Video Recognition project
# Enable "3rd Party ID Integration" for Spotify/YouTube links in results.
ACRCLOUD_ACCESS_KEY=
ACRCLOUD_ACCESS_SECRET=
ACRCLOUD_HOST=identify-us-west-2.acrcloud.com

# ACRCloud Custom Bucket – for unreleased track fingerprinting
# 1. Create a bucket at: https://console.acrcloud.com → Buckets → Custom Files
# 2. Get bucket ID from the bucket settings
# 3. Create a Personal Access Token: Developer Setting → Personal Access Token → Create Token
#    (select "audios" scope for uploading)
ACRCLOUD_BUCKET_NAME=your-bucket-id
ACRCLOUD_BEARER_TOKEN=your-personal-access-token

# ============================================
# Apify – Social Media Scraping (TikTok/Instagram)
# ============================================
# Create an account at https://apify.com/ (free tier includes $5 credits)
# Get your API token from: Settings → Integrations → API Token
APIFY_API_TOKEN=your-apify-api-token

# SoundCloud – required for "Identify at timestamp" with SoundCloud URLs
# Create an app at https://developers.soundcloud.com/ → Your Apps → Create → Client ID
SOUNDCLOUD_CLIENT_ID=

# YouTube – required for "Identify at timestamp" with YouTube URLs
# Run: ./scripts/setup-yt-dlp-macos.sh  (downloads standalone macOS binary to ./bin/yt-dlp)
# Then set YT_DLP_PATH to ./bin/yt-dlp or the absolute path. Or use brew install yt-dlp.
# YT_DLP_PATH=./bin/yt-dlp

# ============================================
# Spotify – Release checking for unreleased tracks
# ============================================
# Create an app at https://developer.spotify.com/dashboard
# Get credentials from your app's settings
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret

# ============================================
# Supabase – Artist/Track Database
# ============================================
# Create a project at https://supabase.com/dashboard
# Get your URL and anon key from: Project Settings → API
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
